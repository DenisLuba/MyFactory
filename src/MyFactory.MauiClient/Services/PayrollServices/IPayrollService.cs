using MyFactory.MauiClient.Models.Payroll;

namespace MyFactory.MauiClient.Services.PayrollServices
{
    public interface IPayrollService
    {
        // Task<List<PayrollGetResponse>?> GetAsync(int periodMonth, int periodYear);
        // Task<PayrollCalculateResponse?> CalculateAsync(DateTime from, DateTime to);
        // Task<PayrollPayResponse?> PayAsync(PayrollPayRequest request);
    }
}