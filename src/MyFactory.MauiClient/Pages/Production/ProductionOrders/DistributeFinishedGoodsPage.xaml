<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Production.ProductionOrders.DistributeFinishedGoodsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Production.ProductionOrders"
	Title="Распределение готовой продукции"
	x:DataType="view_model:DistributeFinishedGoodsPageViewModel">
	<ScrollView>
		<Grid Padding="16" RowDefinitions="Auto,Auto,Auto,Auto,*">
			<Label
				FontAttributes="Bold"
				FontSize="18"
				Text="Распределение готовой продукции" />

			<Grid
				Grid.Row="1"
				ColumnDefinitions="Auto,*"
				ColumnSpacing="8"
				RowDefinitions="Auto,Auto,Auto"
				RowSpacing="6">
				<Label FontAttributes="Bold" Text="Производственный заказ:" />
				<Label Grid.Column="1" Text="{Binding ProductionOrderNumber}" />

				<Label
					Grid.Row="1"
					FontAttributes="Bold"
					Text="Изделие / Кол-во:" />
				<Label
					Grid.Row="1"
					Grid.Column="1"
					Text="{Binding ProductInfo}" />

				<Label
					Grid.Row="2"
					FontAttributes="Bold"
					Text="Осталось распределить:" />
				<Label
					Grid.Row="2"
					Grid.Column="1"
					Text="{Binding RemainingQuantity}" />
			</Grid>

			<Button
				Grid.Row="2"
				Command="{Binding AddDistributionCommand}"
				Text="Добавить склад" />

			<CollectionView
				x:Name="DistributionsCollection"
				Grid.Row="3"
				ItemsSource="{Binding Distributions}"
				SelectionMode="None">
				<CollectionView.ItemTemplate>
					<DataTemplate x:DataType="view_model:DistributeFinishedGoodsPageViewModel+DistributionItemViewModel">
						<Grid
							Padding="0,6"
							ColumnDefinitions="4*,2*,Auto"
							ColumnSpacing="8">
							<Entry
								Grid.Column="0"
								Placeholder="Выберите склад"
								Text="{Binding WarehouseName}"
								Command="{Binding SelectWarehouseCommand}" />
							<Entry
								Grid.Column="1"
								Keyboard="Numeric"
								Placeholder="0"
								Text="{Binding Quantity, StringFormat='{0:F2}'}" />
							<Button
								Grid.Column="2"
								Command="{Binding BindingContext.RemoveDistributionCommand, Source={x:Reference Name=DistributionsCollection}}"
								CommandParameter="{Binding .}"
								Text="Удалить" />
						</Grid>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>

			<Grid Grid.Row="4" ColumnDefinitions="Auto,*">
				<Button Command="{Binding SaveCommand}" Text="Сохранить" />
				<Button
					Grid.Column="1"
					Command="{Binding CancelCommand}"
					HorizontalOptions="End"
					Style="{StaticResource BackButtonStyle}"
					Text="Отмена" />
			</Grid>
		</Grid>
	</ScrollView>
</ContentPage>