<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Users.UserDetailsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Users"
	Title="Карточка пользователя"
	x:DataType="view_model:UserDetailsPageViewModel">
	<ScrollView>
		<VerticalStackLayout Padding="16" Spacing="12">
			<Label
				FontAttributes="Bold"
				FontSize="18"
				Text="{Binding Username}" />

			<Grid
				ColumnDefinitions="Auto,*"
				ColumnSpacing="8"
				RowDefinitions="Auto,Auto,Auto"
				RowSpacing="8">
				<Label FontAttributes="Bold" Text="Роль:" />
				<Picker
					Grid.Column="1"
					ItemDisplayBinding="{Binding Name}"
					ItemsSource="{Binding Roles}"
					SelectedItem="{Binding SelectedRole}" />
				<Label
					Grid.Row="1"
					FontAttributes="Bold"
					Text="Статус:" />
				<HorizontalStackLayout
					Grid.Row="1"
					Grid.Column="1"
					Spacing="8">
					<Label Text="Активен" VerticalTextAlignment="Center" />
					<Switch IsToggled="{Binding IsActive}" />
				</HorizontalStackLayout>
				<Label
					Grid.Row="2"
					FontAttributes="Bold"
					Text="Создан:" />
				<Label
					Grid.Row="2"
					Grid.Column="1"
					Text="{Binding CreatedAt}" />
			</Grid>

			<HorizontalStackLayout Spacing="12">
				<Button Command="{Binding SaveCommand}" Text="Сохранить" />
				<Button Command="{Binding BlockCommand}" Text="Заблокировать" />
			</HorizontalStackLayout>
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>

