<ContentPage
	x:Class="MyFactory.MauiClient.Pages.MaterialsAndSuppliers.Suppliers.SupplierDetailsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.MaterialsAndSuppliers.Suppliers"
	Title="Карточка поставщика"
	x:DataType="view_model:SupplierDetailsPageViewModel">
	<ScrollView>
		<VerticalStackLayout Padding="16" Spacing="12">
			<Label
				FontAttributes="Bold"
				FontSize="20"
				Text="{Binding Name}" />
			<Label Text="{Binding Description}" />
			<Label Text="{Binding Contacts}" />

			<HorizontalStackLayout Spacing="8">
				<Button Command="{Binding EditCommand}" Text="Редактировать" />
				<Button Command="{Binding AddPurchaseCommand}" Text="Добавить закупку" />
				<Button Command="{Binding CreateOrderCommand}" Text="Создать заказ" />
			</HorizontalStackLayout>

			<Border
				Padding="8"
				Stroke="LightGray"
				StrokeThickness="1">
				<VerticalStackLayout Spacing="8">
					<Label FontAttributes="Bold" Text="История закупок" />
					<Grid
						Padding="0,4"
						ColumnDefinitions="*,*,Auto,Auto,Auto"
						ColumnSpacing="8">
						<Label FontAttributes="Bold" Text="Тип" />
						<Label
							Grid.Column="1"
							FontAttributes="Bold"
							Text="Материал" />
						<Label
							Grid.Column="2"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Кол-во" />
						<Label
							Grid.Column="3"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Цена" />
						<Label
							Grid.Column="4"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Дата" />
					</Grid>
					<CollectionView ItemsSource="{Binding PurchaseHistory}" SelectionMode="None">
						<CollectionView.ItemTemplate>
							<DataTemplate>
								<Grid
									Padding="0,6"
									ColumnDefinitions="*,*,Auto,Auto,Auto"
									ColumnSpacing="8">
									<Label Text="{Binding MaterialType}" />
									<Label Grid.Column="1" Text="{Binding MaterialName}" />
									<Label
										Grid.Column="2"
										HorizontalTextAlignment="End"
										Text="{Binding Qty}" />
									<Label
										Grid.Column="3"
										HorizontalTextAlignment="End"
										Text="{Binding UnitPrice}" />
									<Label
										Grid.Column="4"
										HorizontalTextAlignment="End"
										Text="{Binding Date}" />
								</Grid>
							</DataTemplate>
						</CollectionView.ItemTemplate>
					</CollectionView>
				</VerticalStackLayout>
			</Border>
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>

