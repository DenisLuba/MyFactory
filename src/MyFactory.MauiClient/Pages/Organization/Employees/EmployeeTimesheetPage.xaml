<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Organization.Employees.EmployeeTimesheetPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Organization.Employees"
	Title="Табель сотрудника"
	x:DataType="view_model:EmployeeTimesheetPageViewModel">

	<Grid
		Padding="16"
		RowDefinitions="Auto,Auto,*"
		RowSpacing="12">
		<Label
			FontAttributes="Bold"
			FontSize="18"
			Text="Табель учета рабочего времени" />

		<Grid
			Grid.Row="1"
			ColumnDefinitions="Auto,*,Auto"
			ColumnSpacing="8">
			<Label Text="Период:" VerticalTextAlignment="Center" />
			<DatePicker Grid.Column="1" Date="{Binding TimesheetPeriod}" />
			<Button
				Grid.Column="2"
				Command="{Binding AddTimesheetEntryCommand}"
				Text="Добавить" />
		</Grid>

		<Border
			Grid.Row="2"
			Padding="8"
			Stroke="LightGray"
			StrokeThickness="1">
			<Grid RowDefinitions="Auto,*" RowSpacing="8">
				<Grid
					Padding="0,4"
					ColumnDefinitions="2*,2*,6*"
					ColumnSpacing="12">
					<Label FontAttributes="Bold" Text="Дата" />
					<Label
						Grid.Column="1"
						FontAttributes="Bold"
						Text="Часы" />
					<Label
						Grid.Column="2"
						FontAttributes="Bold"
						HorizontalTextAlignment="End"
						Text="Примечание" />
				</Grid>

				<CollectionView
					Grid.Row="1"
					ItemsSource="{Binding TimesheetEntries}"
					SelectionMode="None">
					<CollectionView.ItemTemplate>
						<DataTemplate x:DataType="view_model:EmployeeTimesheetPageViewModel+TimesheetEntryItemViewModel">
							<Grid
								Padding="0,6"
								ColumnDefinitions="2*,2*,6*"
								ColumnSpacing="12">
								<Label Text="{Binding Date}" />
								<Label
									Grid.Column="1"
									HorizontalTextAlignment="End"
									Text="{Binding Hours}" />
								<Label Grid.Column="2" Text="{Binding Comment}" />
							</Grid>
						</DataTemplate>
					</CollectionView.ItemTemplate>
				</CollectionView>
			</Grid>
		</Border>
	</Grid>
</ContentPage>
