<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Organization.Employees.EmployeeTimesheetPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Organization.Employees"
	Title="Табель сотрудника"
	x:DataType="view_model:EmployeeTimesheetPageViewModel">

	<Grid
		Padding="16"
		RowDefinitions="Auto,Auto,Auto,*"
		RowSpacing="12">
		<Label
			FontAttributes="Bold"
			FontSize="18"
			Text="{Binding EmployeeName, StringFormat='Табель сотрудника: {0}'}" />

		<Grid
			Grid.Row="1"
			ColumnDefinitions="Auto,Auto,*"
			RowDefinitions="Auto,Auto"
			ColumnSpacing="8">

			<Label Text="Год" />
			<Picker
				Grid.Row="1"
				ItemsSource="{Binding Years}"
				ItemDisplayBinding="{Binding Value}"
				SelectedItem="{Binding YearFilter, Mode=TwoWay}" />

			<Label Grid.Column="1" Text="Месяц" />
			<Picker
				Grid.Row="1"
				Grid.Column="1"
				ItemsSource="{Binding Months}"
				SelectedIndex="{Binding MonthFilter, Mode=TwoWay}" />

			<Button
				Grid.Column="2"
				Grid.Row="1"
				HorizontalOptions="End"
				Command="{Binding AddTimesheetEntryCommand}"
				Text="{x:StaticResource PlusIcon}" />
		</Grid>

		<Grid
			Grid.Row="2"
			Padding="0,4"
			ColumnDefinitions="2*,2*,6*"
			ColumnSpacing="12">
			<Label FontAttributes="Bold" Text="Дата" />
			<Label
				Grid.Column="1"
				FontAttributes="Bold"
				Text="Часы" />
			<Label
				Grid.Column="2"
				FontAttributes="Bold"
				HorizontalTextAlignment="End"
				Text="Примечание" />
		</Grid>

		<Border Grid.Row="3">
			<CollectionView
				Grid.Row="1"
				ItemsSource="{Binding TimesheetEntries}"
				SelectionMode="None">
				<CollectionView.ItemTemplate>
					<DataTemplate x:DataType="view_model:EmployeeTimesheetPageViewModel+TimesheetEntryItemViewModel">
						<Grid
							Padding="0,6"
							ColumnDefinitions="2*,2*,6*"
							ColumnSpacing="12">
							<Label Text="{Binding Date}" />
							<Label
								Grid.Column="1"
								HorizontalTextAlignment="End"
								Text="{Binding Hours}" />
							<Label Grid.Column="2" Text="{Binding Comment}" />
						</Grid>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
		</Border>
	</Grid>
</ContentPage>
