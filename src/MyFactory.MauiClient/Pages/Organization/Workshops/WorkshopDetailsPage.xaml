<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Organization.Workshops.WorkshopDetailsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:controllers="clr-namespace:MyFactory.MauiClient.Controllers"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Organization.Workshops"
	Title="Карточка участка"
	x:DataType="view_model:WorkshopDetailsPageViewModel">
	<ScrollView>
		<VerticalStackLayout Padding="16" Spacing="12">
			<Label
				FontAttributes="Bold"
				FontSize="18"
				Text="Карточка участка" />

			<Grid
				ColumnDefinitions="Auto,*"
				ColumnSpacing="8"
				RowDefinitions="Auto,Auto,Auto,Auto"
				RowSpacing="8">
				<Label FontAttributes="Bold" Text="Наименование:" />
				<Entry
					Grid.Column="1"
					ReturnType="Next"
					Text="{Binding Name}" />
				<Label
					Grid.Row="1"
					FontAttributes="Bold"
					Text="Код:" />
				<Entry
					Grid.Row="1"
					Grid.Column="1"
					ReturnType="Next"
					Text="{Binding Code}" />
				<Label
					Grid.Row="2"
					FontAttributes="Bold"
					Text="Тип:" />
				<Picker
					Grid.Row="2"
					Grid.Column="1"
					ItemsSource="{Binding Types}"
					SelectedItem="{Binding SelectedType}" />
				<controllers:SwitchLabel
					Grid.Row="3"
					IsEnabled="{Binding IsEditMode}"
					IsToggled="{Binding IsActive}"
					IsVisible="{Binding IsEditMode}"
					Text="Активен:" />
			</Grid>

			<HorizontalStackLayout Spacing="12">
				<Button Command="{Binding SaveCommand}" Text="Сохранить" />
				<Button
					Command="{Binding CancelCommand}"
					Style="{StaticResource BackButtonStyle}"
					Text="Отмена" />
			</HorizontalStackLayout>
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>