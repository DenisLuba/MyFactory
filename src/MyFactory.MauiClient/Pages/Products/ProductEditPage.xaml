<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Products.ProductEditPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Products"
	Title="Редактирование товара"
	x:DataType="view_model:ProductEditPageViewModel">
	<ScrollView>
		<VerticalStackLayout Padding="16" Spacing="12">
			<HorizontalStackLayout Spacing="8">
				<Button Command="{Binding CancelCommand}" Text="Отмена" />
				<Button Command="{Binding SaveCommand}" Text="Сохранить" />
			</HorizontalStackLayout>

			<Grid
				ColumnDefinitions="Auto,*"
				ColumnSpacing="8"
				RowDefinitions="Auto,Auto"
				RowSpacing="8">
				<Label FontAttributes="Bold" Text="Название:" />
				<Entry
					Grid.Column="1"
					Placeholder="Название"
					Text="{Binding Name}" />
				<Label
					Grid.Row="1"
					FontAttributes="Bold"
					Text="План/час:" />
				<Entry
					Grid.Row="1"
					Grid.Column="1"
					Keyboard="Numeric"
					Placeholder="План/час"
					Text="{Binding PlanPerHour}" />
			</Grid>

			<Border
				Padding="8"
				Stroke="LightGray"
				StrokeThickness="1">
				<VerticalStackLayout Spacing="8">
					<Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
						<Label FontAttributes="Bold" Text="СПЕЦИФИКАЦИЯ" />
						<Button
							Grid.Column="1"
							Command="{Binding AddMaterialCommand}"
							Text="+" />
					</Grid>
					<Grid
						Padding="0,4"
						ColumnDefinitions="*,Auto"
						ColumnSpacing="8">
						<Label FontAttributes="Bold" Text="Материал" />
						<Label
							Grid.Column="1"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Количество" />
					</Grid>
					<CollectionView
						x:Name="BomEditCollection"
						ItemsSource="{Binding EditableBom}"
						SelectionMode="None">
						<CollectionView.ItemTemplate>
							<DataTemplate>
								<Grid
									Padding="0,6"
									ColumnDefinitions="*,Auto"
									ColumnSpacing="8">
									<Picker
										ItemDisplayBinding="{Binding Name}"
										ItemsSource="{Binding BindingContext.MaterialOptions, Source={x:Reference Name=BomEditCollection}}"
										SelectedItem="{Binding Material}" />
									<Entry
										Grid.Column="1"
										HorizontalTextAlignment="End"
										Keyboard="Numeric"
										Text="{Binding Quantity}" />
								</Grid>
							</DataTemplate>
						</CollectionView.ItemTemplate>
					</CollectionView>
				</VerticalStackLayout>
			</Border>

			<Border
				Padding="8"
				Stroke="LightGray"
				StrokeThickness="1">
				<VerticalStackLayout Spacing="8">
					<Label FontAttributes="Bold" Text="Производственные затраты" />
					<Grid
						Padding="0,4"
						ColumnDefinitions="*,Auto,Auto,Auto,Auto"
						ColumnSpacing="8">
						<Label FontAttributes="Bold" Text="Цех" />
						<Label
							Grid.Column="1"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Раскрой" />
						<Label
							Grid.Column="2"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Пошив" />
						<Label
							Grid.Column="3"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Упаковка" />
						<Label
							Grid.Column="4"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Прочее" />
					</Grid>
					<CollectionView
						x:Name="ProductionCostsEditCollection"
						ItemsSource="{Binding EditableProductionCosts}"
						SelectionMode="None">
						<CollectionView.ItemTemplate>
							<DataTemplate>
								<Grid
									Padding="0,6"
									ColumnDefinitions="*,Auto,Auto,Auto,Auto"
									ColumnSpacing="8">
									<Picker
										ItemDisplayBinding="{Binding Name}"
										ItemsSource="{Binding BindingContext.DepartmentOptions, Source={x:Reference Name=ProductionCostsEditCollection}}"
										SelectedItem="{Binding Department}" />
									<Entry
										Grid.Column="1"
										HorizontalTextAlignment="End"
										Keyboard="Numeric"
										Text="{Binding Cutting}" />
									<Entry
										Grid.Column="2"
										HorizontalTextAlignment="End"
										Keyboard="Numeric"
										Text="{Binding Sewing}" />
									<Entry
										Grid.Column="3"
										HorizontalTextAlignment="End"
										Keyboard="Numeric"
										Text="{Binding Packaging}" />
									<Entry
										Grid.Column="4"
										HorizontalTextAlignment="End"
										Keyboard="Numeric"
										Text="{Binding Other}" />
								</Grid>
							</DataTemplate>
						</CollectionView.ItemTemplate>
					</CollectionView>
				</VerticalStackLayout>
			</Border>
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>

