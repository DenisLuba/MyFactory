<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Products.ProductCostsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Products"
	Title="{Binding Name}"
	x:DataType="view_model:ProductCostsPageViewModel">

	<Grid
		Padding="16"
		RowDefinitions="Auto,*,Auto"
		RowSpacing="12">
		<Grid
			Padding="16"
			ColumnDefinitions="4*,1*,1*,1*,1*,1*"
			ColumnSpacing="8">
			<Label FontAttributes="Bold" Text="Цех" />
			<Label
				Grid.Column="1"
				FontAttributes="Bold"
				HorizontalTextAlignment="End"
				Text="Раскрой" />
			<Label
				Grid.Column="2"
				FontAttributes="Bold"
				HorizontalTextAlignment="End"
				Text="Пошив" />
			<Label
				Grid.Column="3"
				FontAttributes="Bold"
				HorizontalTextAlignment="End"
				Text="Упаковка" />
			<Label
				Grid.Column="4"
				FontAttributes="Bold"
				HorizontalTextAlignment="End"
				Text="Прочее" />
			<Label
				Grid.Column="5"
				FontAttributes="Bold"
				HorizontalTextAlignment="End"
				Text="Всего" />
		</Grid>

		<Border Grid.Row="1">
			<CollectionView ItemsSource="{Binding Items}" SelectionMode="None">
				<CollectionView.ItemTemplate>
					<DataTemplate x:DataType="view_model:ProductCostsPageViewModel+DepartmentCostViewModel">
						<Grid
							Padding="0,6"
							ColumnDefinitions="4*,1*,1*,1*,1*,1*"
							ColumnSpacing="8">
							<Label Text="{Binding Department}" />
							<Label
								Grid.Column="1"
								HorizontalTextAlignment="End"
								Text="{Binding Cutting}" />
							<Label
								Grid.Column="2"
								HorizontalTextAlignment="End"
								Text="{Binding Sewing}" />
							<Label
								Grid.Column="3"
								HorizontalTextAlignment="End"
								Text="{Binding Packaging}" />
							<Label
								Grid.Column="4"
								HorizontalTextAlignment="End"
								Text="{Binding Other}" />
							<Label
								Grid.Column="5"
								HorizontalTextAlignment="End"
								Text="{Binding Total}" />
						</Grid>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
		</Border>

		<HorizontalStackLayout Grid.Row="2" Spacing="8">
			<Button Command="{Binding BackCommand}" Text="Назад" />
		</HorizontalStackLayout>
	</Grid>
</ContentPage>
