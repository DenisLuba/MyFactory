<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Products.ProductDetailsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Products"
	Title="Карточка товара"
	x:DataType="view_model:ProductDetailsPageViewModel">
	<ScrollView>
		<VerticalStackLayout Padding="16" Spacing="12">
			<HorizontalStackLayout Spacing="8">
				<Button Command="{Binding BackCommand}" Text="Назад" />
				<Button Command="{Binding EditCommand}" Text="Редактировать" />
				<Button Command="{Binding DeleteCommand}" Text="Удалить" />
			</HorizontalStackLayout>

			<Label
				FontAttributes="Bold"
				FontSize="20"
				Text="{Binding Name}" />

			<Grid
				ColumnDefinitions="Auto,*"
				ColumnSpacing="8"
				RowDefinitions="Auto,Auto"
				RowSpacing="8">
				<Label FontAttributes="Bold" Text="План пошива в смену:" />
				<Label Grid.Column="1" Text="{Binding PlanPerHour}" />
				<Label
					Grid.Row="1"
					FontAttributes="Bold"
					Text="Себестоимость:" />
				<Label
					Grid.Row="1"
					Grid.Column="1"
					Text="{Binding TotalCost}" />
			</Grid>

			<Border
				Padding="8"
				Stroke="LightGray"
				StrokeThickness="1">
				<VerticalStackLayout Spacing="6">
					<Label FontAttributes="Bold" Text="Себестоимость" />
					<Grid
						ColumnDefinitions="Auto,*"
						ColumnSpacing="8"
						RowDefinitions="Auto,Auto"
						RowSpacing="4">
						<Label FontAttributes="Bold" Text="Материалы:" />
						<Label Grid.Column="1" Text="{Binding MaterialCost}" />
						<Label
							Grid.Row="1"
							FontAttributes="Bold"
							Text="Производство:" />
						<Label
							Grid.Row="1"
							Grid.Column="1"
							Text="{Binding ProductionCost}" />
					</Grid>
				</VerticalStackLayout>
			</Border>

			<Border
				Padding="8"
				Stroke="LightGray"
				StrokeThickness="1">
				<VerticalStackLayout Spacing="8">
					<Label FontAttributes="Bold" Text="СПЕЦИФИКАЦИЯ (BOM)" />
					<Grid
						Padding="0,4"
						ColumnDefinitions="*,Auto,Auto"
						ColumnSpacing="8">
						<Label FontAttributes="Bold" Text="Материал" />
						<Label
							Grid.Column="1"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Количество" />
						<Label
							Grid.Column="2"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Цена" />
					</Grid>
					<CollectionView
						x:Name="BomCollection"
						ItemsSource="{Binding Bom}"
						SelectionMode="None">
						<CollectionView.ItemTemplate>
							<DataTemplate>
								<Grid
									Padding="0,6"
									ColumnDefinitions="*,Auto,Auto"
									ColumnSpacing="8">
									<Label Text="{Binding MaterialName}" />
									<Label
										Grid.Column="1"
										HorizontalTextAlignment="End"
										Text="{Binding Quantity}" />
									<Label
										Grid.Column="2"
										HorizontalTextAlignment="End"
										Text="{Binding Price}" />
								</Grid>
							</DataTemplate>
						</CollectionView.ItemTemplate>
					</CollectionView>
				</VerticalStackLayout>
			</Border>

			<Border
				Padding="8"
				Stroke="LightGray"
				StrokeThickness="1">
				<VerticalStackLayout Spacing="8">
					<Label FontAttributes="Bold" Text="Производственные затраты" />
					<Grid
						Padding="0,4"
						ColumnDefinitions="*,Auto,Auto,Auto,Auto,Auto"
						ColumnSpacing="8">
						<Label FontAttributes="Bold" Text="Цех" />
						<Label
							Grid.Column="1"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Раскрой" />
						<Label
							Grid.Column="2"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Пошив" />
						<Label
							Grid.Column="3"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Упаковка" />
						<Label
							Grid.Column="4"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Прочее" />
						<Label
							Grid.Column="5"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Всего" />
					</Grid>
					<CollectionView
						x:Name="ProductionCostsCollection"
						ItemsSource="{Binding ProductionCosts}"
						SelectionMode="None">
						<CollectionView.ItemTemplate>
							<DataTemplate>
								<Grid
									Padding="0,6"
									ColumnDefinitions="*,Auto,Auto,Auto,Auto,Auto"
									ColumnSpacing="8">
									<Label Text="{Binding Department}" />
									<Label
										Grid.Column="1"
										HorizontalTextAlignment="End"
										Text="{Binding Cutting}" />
									<Label
										Grid.Column="2"
										HorizontalTextAlignment="End"
										Text="{Binding Sewing}" />
									<Label
										Grid.Column="3"
										HorizontalTextAlignment="End"
										Text="{Binding Packaging}" />
									<Label
										Grid.Column="4"
										HorizontalTextAlignment="End"
										Text="{Binding Other}" />
									<Label
										Grid.Column="5"
										HorizontalTextAlignment="End"
										Text="{Binding Total}" />
								</Grid>
							</DataTemplate>
						</CollectionView.ItemTemplate>
					</CollectionView>
				</VerticalStackLayout>
			</Border>

			<Border
				Padding="8"
				Stroke="LightGray"
				StrokeThickness="1">
				<VerticalStackLayout Spacing="8">
					<Label FontAttributes="Bold" Text="Остатки на складах" />
					<Grid
						Padding="0,4"
						ColumnDefinitions="*,Auto"
						ColumnSpacing="8">
						<Label FontAttributes="Bold" Text="Склад" />
						<Label
							Grid.Column="1"
							FontAttributes="Bold"
							HorizontalTextAlignment="End"
							Text="Доступно" />
					</Grid>
					<CollectionView ItemsSource="{Binding Warehouses}" SelectionMode="None">
						<CollectionView.ItemTemplate>
							<DataTemplate>
								<Grid
									Padding="0,6"
									ColumnDefinitions="*,Auto"
									ColumnSpacing="8">
									<Label Text="{Binding WarehouseName}" />
									<Label
										Grid.Column="1"
										HorizontalTextAlignment="End"
										Text="{Binding Available}" />
								</Grid>
							</DataTemplate>
						</CollectionView.ItemTemplate>
					</CollectionView>
				</VerticalStackLayout>
			</Border>
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>

