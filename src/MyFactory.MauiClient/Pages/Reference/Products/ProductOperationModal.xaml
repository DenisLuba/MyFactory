<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyFactory.MauiClient.Pages.Reference.Products.ProductOperationModal"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Reference.Products"
    x:DataType="view_model:ProductOperationModalViewModel"
    Title="Операция изделия">

    <ContentPage.Resources>
        <Style TargetType="Border" x:Key="FormBorderStyle">
            <Setter Property="Stroke" Value="{AppThemeBinding Light=#E5E7EB, Dark=#374151}" />
            <Setter Property="StrokeThickness" Value="1" />
            <Setter Property="Background" Value="{AppThemeBinding Light=#FFFFFF, Dark=#1F2937}" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="StrokeShape">
                <Setter.Value>
                    <RoundRectangle CornerRadius="12" />
                </Setter.Value>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18">
            <Label FontAttributes="Bold" FontSize="20" Text="{Binding ProductName}" />

            <Border Style="{StaticResource FormBorderStyle}">
                <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="16">
                    <VerticalStackLayout>
                        <Label FontSize="12" Text="Операция" TextColor="{DynamicResource Gray500}" />
                        <Entry Placeholder="Например: Пошив изделия" Text="{Binding Operation}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="1">
                        <Label FontSize="12" Text="Время, мин" TextColor="{DynamicResource Gray500}" />
                        <Entry Keyboard="Numeric" Placeholder="0,0" Text="{Binding MinutesText}" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="2">
                        <Label FontSize="12" Text="Стоимость" TextColor="{DynamicResource Gray500}" />
                        <Entry Keyboard="Numeric" Placeholder="0,00" Text="{Binding CostText}" />
                    </VerticalStackLayout>
                </Grid>
            </Border>

            <ActivityIndicator IsRunning="{Binding IsSubmitting}" IsVisible="{Binding IsSubmitting}" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button
                    BackgroundColor="{DynamicResource Gray200}"
                    Command="{Binding CancelCommand}"
                    IsEnabled="{Binding IsSubmitting, Converter={StaticResource InvertedBoolConverter}}"
                    Text="Отмена"
                    TextColor="{AppThemeBinding Light=Black, Dark=White}" />
                <Button
                    Grid.Column="1"
                    Command="{Binding SubmitCommand}"
                    IsEnabled="{Binding IsSubmitting, Converter={StaticResource InvertedBoolConverter}}"
                    Text="Сохранить" />
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
