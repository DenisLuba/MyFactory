<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyFactory.MauiClient.Pages.Reference.Workshops.WorkshopCardPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:converters="clr-namespace:MyFactory.MauiClient.Converters"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Reference.Workshops"
    x:DataType="view_model:WorkshopCardPageViewModel"
    Title="Карточка цеха">

    <ContentPage.Resources>
        <Color x:Key="SecondaryText">#6B7280</Color>
        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        <Style x:Key="SectionBorder" TargetType="Border">
            <Setter Property="Stroke" Value="{AppThemeBinding Light=#E5E7EB, Dark=#374151}" />
            <Setter Property="Background" Value="{AppThemeBinding Light=#FFFFFF, Dark=#1F2937}" />
            <Setter Property="StrokeShape">
                <Setter.Value>
                    <RoundRectangle CornerRadius="12" />
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="16,0,16,16" />
        </Style>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Сохранить" Command="{Binding SaveWorkshopCommand}" Priority="0" Order="Primary" />
    </ContentPage.ToolbarItems>

    <RefreshView Command="{Binding LoadWorkshopCommand}" IsRefreshing="{Binding IsBusy}">
        <ScrollView>
            <VerticalStackLayout Spacing="16" Padding="0,16">
                <HorizontalStackLayout Spacing="8" HorizontalOptions="Center" VerticalOptions="Center">
                    <Label FontAttributes="Bold" FontSize="20" Text="Цех" />
                    <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
                </HorizontalStackLayout>

                <Border Style="{StaticResource SectionBorder}">
                    <VerticalStackLayout Spacing="12">
                        <VerticalStackLayout>
                            <Label Text="Название" FontAttributes="Bold" />
                            <Entry Text="{Binding Name}" Placeholder="Введите название" />
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <Label Text="Тип" FontAttributes="Bold" />
                            <Picker
                                ItemsSource="{Binding WorkshopTypes}"
                                ItemDisplayBinding="{Binding .}"
                                SelectedItem="{Binding SelectedType}" />
                        </VerticalStackLayout>

                        <VerticalStackLayout>
                            <Label Text="Статус" FontAttributes="Bold" />
                            <Picker
                                ItemsSource="{Binding Statuses}"
                                ItemDisplayBinding="{Binding .}"
                                SelectedItem="{Binding SelectedStatus}" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Button
                    Margin="16,0"
                    Text="Сохранить"
                    Command="{Binding SaveWorkshopCommand}"
                    IsEnabled="{Binding IsSaving, Converter={StaticResource InvertedBoolConverter}}" />

                <Button
                    Margin="16,0"
                    Text="История расходов"
                    Command="{Binding OpenExpensesCommand}" />
            </VerticalStackLayout>
        </ScrollView>
    </RefreshView>
</ContentPage>
