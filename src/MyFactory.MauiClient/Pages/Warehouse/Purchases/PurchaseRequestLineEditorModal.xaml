<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyFactory.MauiClient.Pages.Warehouse.Purchases.PurchaseRequestLineEditorModal"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Warehouse.Purchases"
    x:DataType="view_model:PurchaseRequestLineEditorViewModel"
    Title="{Binding Title}">

    <ContentPage.Resources>
        <Style TargetType="Border" x:Key="FormBorderStyle">
            <Setter Property="Stroke" Value="{AppThemeBinding Light=#E5E7EB, Dark=#374151}" />
            <Setter Property="StrokeThickness" Value="1" />
            <Setter Property="Background" Value="{AppThemeBinding Light=#FFFFFF, Dark=#111827}" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="StrokeShape">
                <Setter.Value>
                    <RoundRectangle CornerRadius="12" />
                </Setter.Value>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="18">
            <Border Style="{StaticResource FormBorderStyle}">
                <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto" RowSpacing="16">
                    <VerticalStackLayout>
                        <Label FontSize="12" Text="Материал" TextColor="{DynamicResource Gray500}" />
                        <Picker
                            ItemsSource="{Binding Materials}"
                            SelectedItem="{Binding SelectedMaterial}"
                            ItemDisplayBinding="{Binding DisplayName}"
                            Title="Выберите материал" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="1">
                        <Label FontSize="12" Text="Наименование" TextColor="{DynamicResource Gray500}" />
                        <Entry Text="{Binding MaterialName}" Placeholder="Будет заполнено автоматически" IsReadOnly="True" />
                    </VerticalStackLayout>

                    <Grid Grid.Row="2" ColumnDefinitions="*,Auto" ColumnSpacing="12">
                        <VerticalStackLayout>
                            <Label FontSize="12" Text="Количество" TextColor="{DynamicResource Gray500}" />
                            <Entry Text="{Binding QuantityText}" Keyboard="Numeric" Placeholder="0" />
                        </VerticalStackLayout>
                        <VerticalStackLayout Grid.Column="1">
                            <Label FontSize="12" Text="Ед." TextColor="{DynamicResource Gray500}" />
                            <Entry Text="{Binding Unit}" WidthRequest="80" />
                        </VerticalStackLayout>
                    </Grid>

                    <VerticalStackLayout Grid.Row="3">
                        <Label FontSize="12" Text="Цена" TextColor="{DynamicResource Gray500}" />
                        <Entry Text="{Binding PriceText}" Keyboard="Numeric" Placeholder="0" />
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Row="4">
                        <Label FontSize="12" Text="Комментарий" TextColor="{DynamicResource Gray500}" />
                        <Entry Text="{Binding Note}" Placeholder="Например, срочно" />
                    </VerticalStackLayout>
                </Grid>
            </Border>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                <Button
                    BackgroundColor="{DynamicResource Gray200}"
                    Command="{Binding CancelCommand}"
                    Text="Отмена"
                    TextColor="{AppThemeBinding Light=Black, Dark=White}" />
                <Button
                    Grid.Column="1"
                    Command="{Binding SaveCommand}"
                    Text="Сохранить" />
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
