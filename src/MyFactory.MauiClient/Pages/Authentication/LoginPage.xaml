<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Authentication.LoginPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Authentication"
	Title="Вход"
	x:DataType="view_model:LoginPageViewModel">
	<ScrollView>
		<VerticalStackLayout
			Padding="24"
			Spacing="16"
			VerticalOptions="CenterAndExpand">
			<Label
				FontAttributes="Bold"
				FontSize="24"
				HorizontalTextAlignment="Center"
				Text="Sign in" />
			<Entry
				Keyboard="Email"
				Placeholder="Email или логин"
				Text="{Binding Email}" />
			<Entry
				IsPassword="True"
				Placeholder="Пароль"
				Text="{Binding Password}" />
			<Button Command="{Binding LoginCommand}" Text="Login" />
			<ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
			<Label
				HorizontalTextAlignment="Center"
				Text="{Binding ErrorMessage}"
				TextColor="Red" />
			<Button Command="{Binding NavigateToPasswordResetCommand}" Text="Forgot password?" />
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>
