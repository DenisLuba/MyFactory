<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Authentication.LoginPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Authentication"
	Title="Вход"
	x:DataType="view_model:LoginPageViewModel"
	NavigationPage.HasBackButton="False">
	<ScrollView>
		<VerticalStackLayout
			Padding="24"
			Spacing="16"
			VerticalOptions="Center">
			<Label
				FontAttributes="Bold"
				FontSize="24"
				HorizontalTextAlignment="Center"
				Text="Вход" />
			<Entry
				Keyboard="Email"
				Placeholder="Логин"
				ReturnType="Next"
				Text="{Binding Login}" />
			<Entry
				IsPassword="True"
				Placeholder="Пароль"
				ReturnCommand="{Binding LoginCommand}"
				Text="{Binding Password}" />
			<Button Command="{Binding LoginCommand}" Text="Войти" />

			<!-- Optional registration button
			<Button Command="{Binding NavigateToRegisterCommand}" Text="Регистрация" />
			-->

			<ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" />
			<Label
				HorizontalTextAlignment="Center"
				Text="{Binding ErrorMessage}"
				TextColor="Red" />
			<!--<Button Command="{Binding NavigateToPasswordResetCommand}" Text="Сбросить пароль?" />-->
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>
