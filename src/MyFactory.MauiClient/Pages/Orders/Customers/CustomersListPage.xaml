<ContentPage
	x:Class="MyFactory.MauiClient.Pages.Orders.Customers.CustomersListPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.Orders.Customers"
	Title="Заказчики"
	x:DataType="view_model:CustomersListPageViewModel">

	<Grid
		Padding="16"
		RowDefinitions="Auto,Auto,*"
		RowSpacing="12">
		<Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
			<Label
				FontAttributes="Bold"
				FontSize="18"
				Text="Заказчики" />
			<Button
				Grid.Column="1"
				Command="{Binding AddCommand}"
				Text="+" />
		</Grid>
		<Grid
			Grid.Row="1"
			Padding="16"
			ColumnDefinitions="3*,2*,2*,2*,1*"
			ColumnSpacing="8">
			<Label FontAttributes="Bold" Text="Название" />
			<Label
				Grid.Column="1"
				FontAttributes="Bold"
				Text="Телефон" />
			<Label
				Grid.Column="2"
				FontAttributes="Bold"
				Text="Email" />
			<Label
				Grid.Column="3"
				FontAttributes="Bold"
				Text="Адрес" />
		</Grid>
		<Border Grid.Row="2">
			<CollectionView
				x:Name="CustomersCollection"
				ItemsSource="{Binding Customers}"
				SelectionMode="None">
				<CollectionView.ItemTemplate>
					<DataTemplate x:DataType="view_model:CustomersListPageViewModel+CustomerItemViewModel">
						<SwipeView>
							<SwipeView.RightItems>
								<SwipeItems Mode="Reveal">
									<SwipeItem
										BackgroundColor="LightGray"
										Command="{Binding BindingContext.EditCommand, Source={x:Reference Name=CustomersCollection}}"
										CommandParameter="{Binding .}"
										Text="Редактировать" />
									<SwipeItem
										BackgroundColor="Tomato"
										Command="{Binding BindingContext.DeleteCommand, Source={x:Reference Name=CustomersCollection}}"
										CommandParameter="{Binding .}"
										Text="Удалить" />
								</SwipeItems>
							</SwipeView.RightItems>
							<Grid
								ColumnDefinitions="3*,2*,2*,2*,1*"
								ColumnSpacing="8"
								Style="{StaticResource SelectableItemGridStyle}">
								<Grid.GestureRecognizers>
									<TapGestureRecognizer Command="{Binding BindingContext.OpenDetailsCommand, Source={x:Reference Name=CustomersCollection}}" CommandParameter="{Binding .}" />
								</Grid.GestureRecognizers>
								<Label Text="{Binding Name}" />
								<Label Grid.Column="1" Text="{Binding Phone}" />
								<Label Grid.Column="2" Text="{Binding Email}" />
								<Label Grid.Column="3" Text="{Binding Address}" />
								<Label
									Grid.Column="4"
									Padding="0,0,12,0"
									HorizontalTextAlignment="End"
									Text="Удалить"
									TextColor="Tomato">
									<Label.GestureRecognizers>
										<TapGestureRecognizer Command="{Binding BindingContext.DeleteCommand, Source={x:Reference Name=CustomersCollection}}" CommandParameter="{Binding .}" />
									</Label.GestureRecognizers>
								</Label>
							</Grid>
						</SwipeView>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
		</Border>
	</Grid>
</ContentPage>
