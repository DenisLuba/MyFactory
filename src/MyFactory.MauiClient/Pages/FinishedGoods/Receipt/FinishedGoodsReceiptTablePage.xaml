<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MyFactory.MauiClient.Pages.FinishedGoods.Receipt.FinishedGoodsReceiptTablePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:MyFactory.MauiClient.Converters"
    xmlns:ui="clr-namespace:MyFactory.MauiClient.UIModels.FinishedGoods"
    xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels.FinishedGoods.Receipt"
    x:DataType="view_model:FinishedGoodsReceiptTablePageViewModel"
    Title="Оприходование готовой продукции">

    <ContentPage.Resources>
        <Color x:Key="TextSecondaryColor">#6B7280</Color>
        <Color x:Key="PrimaryColor">#2563EB</Color>

        <Style x:Key="CaptionLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="TextColor" Value="{StaticResource TextSecondaryColor}" />
        </Style>

        <Style x:Key="CardFrameStyle" TargetType="Frame">
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="HasShadow" Value="False" />
            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FFFFFF, Dark=#1F2937}" />
            <Setter Property="BorderColor" Value="{AppThemeBinding Light=#E5E7EB, Dark=#374151}" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="14" />
        </Style>

        <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
    </ContentPage.Resources>

    <Grid Padding="16" RowDefinitions="Auto,*" RowSpacing="12">
        <HorizontalStackLayout Spacing="12" VerticalOptions="Center">
            <Label
                FontAttributes="Bold"
                FontSize="18"
                Text="Документы оприходования"
                VerticalOptions="Center" />
            <ActivityIndicator
                IsRunning="{Binding IsBusy}"
                IsVisible="{Binding IsBusy}"
                VerticalOptions="Center" />
            <Button
                Command="{Binding RefreshCommand}"
                HorizontalOptions="EndAndExpand"
                IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                Text="Обновить" />
        </HorizontalStackLayout>

        <RefreshView Grid.Row="1" Command="{Binding RefreshCommand}" IsRefreshing="{Binding IsBusy}">
            <CollectionView ItemsSource="{Binding Receipts}" SelectionMode="None">
                <CollectionView.EmptyView>
                    <Label
                        HorizontalOptions="Center"
                        Padding="24"
                        Text="Нет документов"
                        TextColor="{StaticResource TextSecondaryColor}"
                        VerticalOptions="Center" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="ui:FinishedGoodsReceiptItem">
                        <Frame Style="{StaticResource CardFrameStyle}">
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="12">
                                <Label FontAttributes="Bold" Text="{Binding ProductName}" />
                                <Label
                                    Grid.Column="1"
                                    FontAttributes="Bold"
                                    HorizontalTextAlignment="End"
                                    Text="{Binding Sum, StringFormat='{}{0:N0} ₽'}" />

                                <Label
                                    Grid.Row="1"
                                    Style="{StaticResource CaptionLabelStyle}"
                                    Text="{Binding Date, StringFormat='{}{0:dd.MM.yyyy}'}" />
                                <Label
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    HorizontalTextAlignment="End"
                                    Text="{Binding Warehouse}"
                                    TextColor="{StaticResource TextSecondaryColor}" />

                                <StackLayout Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12">
                                    <Label Text="{Binding Quantity, StringFormat='Кол-во: {0} шт.'}" />
                                    <Label
                                        Text="{Binding UnitPrice, StringFormat='Цена: {0:N2} ₽'}"
                                        TextColor="{StaticResource TextSecondaryColor}" />
                                </StackLayout>
                            </Grid>
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenReceiptCommand}" CommandParameter="{Binding .}" />
                            </Frame.GestureRecognizers>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
