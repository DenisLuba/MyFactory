<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
	x:Class="MyFactory.MauiClient.Views.MaterialsPage"
	xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:view_model="clr-namespace:MyFactory.MauiClient.ViewModels"
	x:DataType="view_model:MaterialsPageViewModel"
	Title="Материалы">
	
	<CollectionView>
		<CollectionView.Header>
			<Grid ColumnDefinitions="*,*,*,*,*">
				<Label Text="Наименование" Grid.Column="0"/>
				<Label Text="Тип" Grid.Column="1"/>
				<Label Text="Ед.изм." Grid.Column="2"/>
				<Label Text="Последняя цена" Grid.Column="3" />
				<Label Text="Статус" Grid.Column="4"/> 
			</Grid>
		</CollectionView.Header>
		
		<CollectionView.ItemTemplate>
			<DataTemplate>
				<Grid ColumnDefinitions="*,*,*,*,*">
					<Label Text="{Binding Name}" Grid.Column="0"/>
					<Label Text="{Binding MaterialType}" Grid.Column="1"/>
					<Label Text="{Binding Unit}" Grid.Column="2"/>
					<Label Text="{Binding LastPrice}" Grid.Column="3"/>
					<Label Text="{Binding Status}" Grid.Column="4"/>
				</Grid>
			</DataTemplate>
		</CollectionView.ItemTemplate>
	</CollectionView>
</ContentPage>